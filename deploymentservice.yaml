apiVersion: v1
kind: Service
metadata:
    labels:
        app: xgb-classifier
        name: jenkins
        namespace: devops-tools
spec:
    ports:
    - name: predict
      port: 3000
      targetPort: 3000
    selector:
      app: xgb-classifier
    type: LoadBalancer
    externalIPs:
    - 54.234.65.219
---
apiVersion: apps/v1
kind: Deployment
metadata:
    labels:
        app: xgb-classifier
        name: jenkins
  namespace: devops-tools
spec:
    selector:
        matchLabels:
            app: xgb-classifier
    replicas: 2
    template:
        metadata:
            labels:
                app: xgb-classifier
        spec:
            containers:
            - image: vaibhavahuja1/xgb_classifier:latest
              imagePullPolicy: IfNotPresent
              name: xgb-classifier
              ports:
              - containerPort: 3000
